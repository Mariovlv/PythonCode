# modulo sys, os, subprocess
# os
"""
import os
dir(os) # ver que se puede hacer
help(os) # ver ayuda
# os

os.system(arg)
	arg('mkdir nombre') - crear una carpeta
	arg('del nombre') - eliminar una carpeta
	arg('dir') - obtener raíz de lo que hay
os.getcwd() - obtener ruta del directorio actual
"""